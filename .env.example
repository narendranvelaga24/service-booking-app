# Environment variables for Service Booking App

# Port for the application to run on
PORT=3001

# Secret key for session management.
# Replace with a long, random string for security.
# You can generate one using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your_very_secret_key_here_change_it

# Node environment (development, production, test)
NODE_ENV=development

# Database file name (if you need to change it from main.db)
# DB_NAME=main.db

# Optional: Email service credentials (if implementing real email sending)
# MAIL_HOST=smtp.example.com
# MAIL_PORT=587
# MAIL_USER=your_email_username
# MAIL_PASS=your_email_password
# MAIL_FROM_ADDRESS="Service Booking App <no-reply@example.com>"
